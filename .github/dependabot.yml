version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
    open-pull-requests-limit: 5
    versioning-strategy: 'increase'
    # `@types/node` is pinned to the major matching `engines.node` in package.json.
    # Bumping past the engines floor would expose Node API types that don't exist at
    # runtime on the supported Node major, turning typecheck-pass into runtime-fail.
    # Minor and patch updates within the supported major still flow through.
    ignore:
      - dependency-name: '@types/node'
        update-types:
          - 'version-update:semver-major'
    groups:
      prod-deps:
        dependency-type: 'production'
        update-types:
          - 'minor'
          - 'patch'
      dev-deps:
        dependency-type: 'development'
        update-types:
          - 'minor'
          - 'patch'
    commit-message:
      # Hard-code the `(deps)` scope for both prod and dev so PR titles
      # match the commitlint scope-enum. `include: 'scope'` would otherwise
      # append `(deps-dev)` to dev-dep PRs — which is not in scope-enum
      # (consolidated into `deps` per SCAFFOLD.md → Commitlint scopes).
      prefix: 'chore(deps)'
      prefix-development: 'chore(deps)'

  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'weekly'
    open-pull-requests-limit: 3
    groups:
      actions:
        patterns:
          - '*'
    commit-message:
      prefix: 'ci'
      include: 'scope'
